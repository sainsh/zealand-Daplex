const weight = require('./weight');

test('Returns string', () => {
    expect(weight("text")).toBe("text");
});

test('Reads from helpdesk_weight_table', () =>{


    expect(result).toBe(mockData);
});